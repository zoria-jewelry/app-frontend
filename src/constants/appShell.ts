/**
 * Height of the top app bar (header) and the matching strip above the sidebar nav
 * (expand/collapse). Keep in sync visually.
 */
export const APP_HEADER_BAR_HEIGHT = '5rem';

/** Sidebar nav icons + expand/collapse control (px). */
export const SIDEBAR_ICON_FONT_PX = 30;

/**
 * Floor width for the Work Units report area (beside the filter rail).
 * Narrower viewports scroll horizontally instead of compressing into the sidebar.
 */
export const WORK_UNITS_MAIN_CONTENT_MIN_WIDTH_PX = 480;
